{
  "name": "gemini-live-translator",
  "version": "1.0.0",
  "private": true,
  "description": "Android (Capacitor) wrapper for the Gemini Live audio-to-audio translator",
  "scripts": {
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "cap:patch": "node scripts/patch-android.mjs",
    "cap:open": "cap open android",
    "android:build": "npm run cap:sync && npm run cap:patch && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
